#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main(){
    int x1,y1,x2,y2;
    double eucldistance;
    scanf("%d %d %d %d",&x1,&y1,&x2,&y2);
    eucldistance= sqrt(pow(x2-x1,2)+pow(y2-y1,2));
    printf("%.2lf\n",eucldistance);
    
    return 0;
}
